from django.apps import AppConfig


class DisplayEventConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.display_event"
    verbose_name = "Display Event (Hero 등)"
